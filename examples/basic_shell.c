#include <apue_shell.h>
#include <stdio.h>

int main(){
    run_shell();
//    safe_string_arr_t *args = get_args("ls");
//    for(int i = 0; i < args->size; i++){
//        printf("%s\n", safe_string_arr_get(args, i));
//    }
}